version: "1.0"

linter: jetbrains/qodana-cdnet:2025.3-eap

dotnet:
  solution: JobTracker.slnx

exclude:
  - name: All
    paths:
      - wwwroot/lib
      - obj
      - bin
      - Migrations
      - .vs
      - .git

profile:
  name: qodana.recommended
